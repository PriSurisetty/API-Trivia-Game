class Question:

    def __init__(self, q_text, q_answer):
        self.text = q_text
        self.answer = q_answer


"""What this module does:

######################################### CLASS 'Question' #######################################

- Creates a class called 'Question' which holds two parameters, the question and the answer.
- It is called in main.py, where we create a variable called 'new_question', passing in the parameters 'question_text'
and 'question_answer' which were created previously by getting ahold of the keys 'correct_answer'
and 'question'
"""